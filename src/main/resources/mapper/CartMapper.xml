<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dreamdaisy.item.mapper.CartMapper">
    <select id="getCartByMemberId" resultType="com.dreamdaisy.cart.domain.Cart">
        SELECT * FROM cart WHERE member_id = #{memberId}
    </select>

    <insert id="insertCart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart(member_id) VALUES(#{memberId})
    </insert>

    <select id="findItemsByCartId" resultType="com.dreamdaisy.cart.domain.CartItem">
        SELECT * FROM cart_item WHERE cart_id = #{cartId}
    </select>

    <insert id="insertCartItem">
        INSERT INTO cart_item(cart_id, item_id, quantity) VALUES(#{cartId}, #{itemId}, #{quantity})
    </insert>

    <update id="updateCartItem">
        UPDATE cart_item SET quantity = #{quantity} WHERE id = #{id}
    </update>
</mapper>